on:
  workflow_run:
    types:
      - completed
  workflow_dispatch:
jobs:
  deploy:
    name: Deploy to production
    runs-on: ubuntu-latest
    steps:
    - name: Deploy to production
      run: echo "Specific deploy steps..."
